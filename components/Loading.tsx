import React from "react";

const Loading = () => {
  return (
    <div className="h-[calc(100vh-8rem)] w-full bg-primary-1 text-white flex justify-center items-center">
      Loading...
    </div>
  );
};

export default Loading;
