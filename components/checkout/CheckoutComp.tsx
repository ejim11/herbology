import React from "react";
import CheckoutForm from "./CheckoutForm";

const CheckoutComp = () => {
  return (
    <section className="flex w-full py-[7rem] px-[8rem] max-xl:px-[5rem] max-lg:px-[3rem] max-md:px-[2rem] max-md:py-[4rem]">
      <CheckoutForm />
    </section>
  );
};

export default CheckoutComp;
